import { clientBundle } from '../tsdown.client.ts'

/** Browser panel bundle plus the empty Host-side package entry. */
export default clientBundle('@deepseek-ai/dsh-client-ui-open-design', ['lib/types/index.js'])
